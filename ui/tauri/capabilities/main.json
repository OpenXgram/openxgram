{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "OpenXgram main window capabilities (PRD-TAURI-02 보안 — 와일드카드 금지). Phase 2 baseline: frontend 가 실제 사용하는 plugin (dialog, clipboard-manager) 만 활성. notification/store/global-shortcut/stronghold/updater 는 후속 PR 에서 plugin 추가와 함께 복원.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-clear",
    {
      "identifier": "core:webview:allow-create-webview",
      "deny": [{ "label": "*" }]
    }
  ]
}
